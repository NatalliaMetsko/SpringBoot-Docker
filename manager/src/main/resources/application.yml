server:
  port: 8083
eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_IP:localhost}:8761/eureka/
url:
  inventory: http://${INVENTORY_IP:localhost}:8082/api/v1/inventory
  catalog: http://${CATALOG_IP:localhost}:8081/api/v1/catalog/offers